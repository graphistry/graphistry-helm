{{- if eq (include "graphistry.tier.full" .) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: pivot-configmap
  name: pivot-configmap
data:
  config.json: |
        {  }
{{- end }}
